[
  {simple_gossip, [
    % Can be auto, sync, async. This sets the event hub notification mode. auto means async by default but if the events starts to stack up switch to sync.
    {event_mode, auto}
  ]}
].